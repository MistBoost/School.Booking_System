<Page
    x:Class="School.OnlineBookingSystem.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewModels="using:School.OnlineBookingSystem.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d">
    <Page.DataContext>
        <viewModels:LoginPageVm x:Name="LoginPageVm"></viewModels:LoginPageVm>

    </Page.DataContext>
    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <NavigationThemeTransition.DefaultNavigationTransitionInfo>
                    <EntranceNavigationTransitionInfo />
                </NavigationThemeTransition.DefaultNavigationTransitionInfo>
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Width="230" Height="328" Margin="525,185,525,207">
            <SymbolIcon Symbol="Admin" Height="56" Margin="70,0,65,0"/>
            <TextBox PlaceholderText="Username"
                     Margin="10"
                     Text="{x:Bind LoginPageVm.RawUsername, Mode=TwoWay}"/>
            <PasswordBox x:Name="passwordBox" PlaceholderText="Password"
                         Margin="10"
                         Password="{x:Bind LoginPageVm.RawPassword, Mode=TwoWay}"/>
            <Button Content="Sign In"
                    Margin="10"
                    HorizontalAlignment="Stretch"
                    Command="{x:Bind LoginPageVm.LoginCommand, Mode=OneWay}"/>
            <HyperlinkButton Content="Create new account"
                             HorizontalAlignment="Center"
                             Command="{x:Bind LoginPageVm.RegisterAccount}"/>
            <HyperlinkButton Content="Continue without Logging In"
                             HorizontalAlignment="Center"
                             Command="{x:Bind LoginPageVm.SkipToMainPage}"/>
            <TextBlock Text="{x:Bind LoginPageVm.StatusMessages, Mode=TwoWay}"
                       Padding="5"
                       Foreground="Red"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>



